npm install -- to install all the required modules

For DataBase -->
	Schema is provided in database folder
	Upload the data from csv files
	Configuration can be done from config/db.config.js

For Secret key JWT -->
	Key is in config/db.config.js file

For user to access token is required which will genrate on register api call 

It will make an entry in user_details table

To start node server -->


node index.js
 